const tables={partnership:'partnership_enquiries',volunteer:'volunteer_applications',professional:'professional_applications',impact:'impact_metrics'};
export default async function handler(req,res){const token=(req.headers.authorization||'').replace(/^Bearer\s+/i,'');const url=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,anon=process.env.SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,service=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!token||!url||!anon||!service)return res.status(401).json({error:'Authorised access required.'});const auth=await fetch(`${url}/auth/v1/user`,{headers:{apikey:anon,Authorization:`Bearer ${token}`}});if(!auth.ok)return res.status(401).json({error:'Sign-in expired.'});const user=await auth.json();const h={apikey:service,Authorization:`Bearer ${service}`,'Accept-Profile':'beautiful_you'};const m=await fetch(`${url}/rest/v1/members?user_id=eq.${user.id}&status=eq.active&select=role`,{headers:h});const roles=m.ok?await m.json():[];if(!roles.some(x=>['owner','admin','manager'].includes(x.role)))return res.status(403).json({error:'Operations access denied.'});const table=tables[req.query?.area];if(!table)return res.status(400).json({error:'Invalid operations area.'});const endpoint=`${url}/rest/v1/${table}`;const headers={...h,'Content-Type':'application/json','Content-Profile':'beautiful_you',Prefer:'return=representation'};if(req.method==='GET'){const r=await fetch(`${endpoint}?select=*&order=created_at.desc`,{headers:h});return res.status(r.status).json(await r.json())}if(req.method==='PATCH'){const r=await fetch(`${endpoint}?id=eq.${req.body?.id}`,{method:'PATCH',headers,body:JSON.stringify({status:req.body?.status,assigned_to:req.body?.assigned_to||null})});return res.status(r.status).json(await r.json())}return res.status(405).json({error:'Method not allowed'})}
