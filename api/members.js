export default async function handler(req,res){const token=(req.headers.authorization||'').replace(/^Bearer\s+/i,'');const url=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,anon=process.env.SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,service=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!token||!url||!anon||!service)return res.status(401).json({error:'Authorised access required.'});const auth=await fetch(`${url}/auth/v1/user`,{headers:{apikey:anon,Authorization:`Bearer ${token}`}});if(!auth.ok)return res.status(401).json({error:'Sign-in expired.'});const user=await auth.json();const headers={apikey:service,Authorization:`Bearer ${service}`,'Accept-Profile':'beautiful_you'};const owner=await fetch(`${url}/rest/v1/members?user_id=eq.${user.id}&role=eq.owner&status=eq.active&select=id`,{headers});if(!owner.ok||(await owner.json()).length===0)return res.status(403).json({error:'Owner access required.'});const endpoint=`${url}/rest/v1/members`;if(req.method==='GET'){const r=await fetch(`${endpoint}?select=*%2Cuser_id&order=created_at.desc`,{headers});return res.status(r.status).json(await r.json())}if(!['PATCH','DELETE'].includes(req.method)||!req.body?.id)return res.status(400).json({error:'Member and action are required.'});if(req.body.id===user.id)return res.status(400).json({error:'The owner account cannot be changed here.'});const r=await fetch(`${endpoint}?id=eq.${req.body.id}`,{method:req.method,headers:{...headers,'Content-Type':'application/json','Content-Profile':'beautiful_you',Prefer:'return=representation'},body:req.method==='DELETE'?undefined:JSON.stringify({role:req.body.role,status:req.body.status})});return res.status(r.status).json(await r.json())}
