const tables={partnership:'partnership_enquiries',volunteer:'volunteer_applications',professional:'professional_applications'};
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {kind,...input}=req.body||{},table=tables[kind];if(!table||!input.email||!input.message)return res.status(400).json({error:'Please complete the required fields.'});const url=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return res.status(500).json({error:'Enquiries are not configured.'});const payload=kind==='partnership'?{organisation_name:input.organisation_name,contact_name:input.contact_name,email:input.email,message:input.message}:{name:input.name||input.contact_name,email:input.email,message:input.message,professional_category:input.professional_category||'General'};const r=await fetch(`${url}/rest/v1/${table}`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json','Content-Profile':'beautiful_you',Prefer:'return=minimal'},body:JSON.stringify(payload)});if(!r.ok)return res.status(500).json({error:'We could not send your enquiry.'});return res.status(200).json({message:'Thank you. Your enquiry was received.'})}
