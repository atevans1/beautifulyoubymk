export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {contact,message,urgent}=req.body||{};if(!contact||!message)return res.status(400).json({error:'Please provide contact details and a message.'});const url=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return res.status(500).json({error:'Help requests are not configured yet.'});const email=contact.includes('@')?contact:null;const phone=email?null:contact;const r=await fetch(`${url}/rest/v1/help_requests`,{method:'POST',headers:{apikey:key,'Content-Type':'application/json','Content-Profile':'beautiful_you',Prefer:'return=minimal'},body:JSON.stringify({safe_email:email,safe_phone:phone,preferred_contact_method:email?'email':'phone',support_needed:message,short_description:message,immediate_safety_concern:urgent==='true'})});if(!r.ok)return res.status(500).json({error:'We could not send your request. Please try again when safe.'});return res.status(200).json({message:'Your request was sent confidentially. We will respond when it is safe to do so.'})}
